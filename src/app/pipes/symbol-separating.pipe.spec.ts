import { SymbolSeparatingPipe } from './symbol-separating.pipe';

describe('SymbolSeparatingPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolSeparatingPipe();
    expect(pipe).toBeTruthy();
  });
});
