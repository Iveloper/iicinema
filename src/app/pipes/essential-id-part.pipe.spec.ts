import { EssentialIdPartPipe } from './essential-id-part.pipe';

describe('EssentialIdPartPipe', () => {
  it('create an instance', () => {
    const pipe = new EssentialIdPartPipe();
    expect(pipe).toBeTruthy();
  });
});
