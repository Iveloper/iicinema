import { SpaceBigIntegersPipe } from './space-big-integers.pipe';

describe('SpaceBigIntegersPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceBigIntegersPipe();
    expect(pipe).toBeTruthy();
  });
});
